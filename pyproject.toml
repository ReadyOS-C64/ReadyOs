[project]
name = "readyos-tools"
version = "0.1.0"
description = "Automation and utility scripts for ReadyOS/VICE workflows"
requires-python = ">=3.9"
dependencies = [
  "pyyaml>=6.0.3",
]

[tool.uv]
package = false

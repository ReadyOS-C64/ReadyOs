# Linker configuration for Ready OS Applications
# Runtime window is $1000-$C7FF ($B800 = 47,104 bytes).
# ReadyOS metadata is authoritative in the ReadyOS REU bank; only the resident
# shim occupies $C800-$C9FF.

SYMBOLS {
    __LOADADDR__:  type = import;
    __STACKSIZE__: type = weak, value = $0070;
    __HIMEM__:     type = weak, value = $C800;
}

MEMORY {
    ZP:       file = "", start = $0002, size = $001A, type = rw, define = yes;
    LOADADDR: file = %O, start = $0FFE, size = $0002;
    MAIN:     file = %O, start = $1000, size = $B800, type = rw, define = yes;
    BSS:      file = "", start = __ONCE_RUN__ + __ONCE_SIZE__,
              size = __HIMEM__ - __STACKSIZE__ - (__ONCE_RUN__ + __ONCE_SIZE__);
}

SEGMENTS {
    ZEROPAGE:  load = ZP, type = zp;
    LOADADDR:  load = LOADADDR, type = ro;
    STARTUP:   load = MAIN, type = ro;
    LOWCODE:   load = MAIN, type = ro, optional = yes;
    CODE:      load = MAIN, type = ro;
    RODATA:    load = MAIN, type = ro;
    DATA:      load = MAIN, type = rw;
    INIT:      load = MAIN, type = rw, optional = yes;
    ONCE:      load = MAIN, type = ro, optional = yes, define = yes;
    BSS:       load = BSS, type = bss, define = yes;
}

FEATURES {
    CONDES: type = constructor,
            label = __CONSTRUCTOR_TABLE__,
            count = __CONSTRUCTOR_COUNT__,
            segment = ONCE;
    CONDES: type = destructor,
            label = __DESTRUCTOR_TABLE__,
            count = __DESTRUCTOR_COUNT__,
            segment = RODATA;
}
